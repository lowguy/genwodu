<!--index.wxml-->
<view class="section">
  <map id="myMap" longitude="{{longitude}}" latitude="{{latitude}}"  markers="{{markers}}" bindmarkertap="markertap" controls="{{controls}}" bindcontroltap="controltap" show-location style="height:{{mapheight}}px;width:100%;" scale="16"/>
</view>

